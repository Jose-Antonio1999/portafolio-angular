export interface infoPagina {
  // el simbolo ? es para ver si el valor va a venir, puede ser que no venga o puede ser que venga
  titulo?: string;
  email?: string;
  nombre_corto?: string;
  pagina_autor?: string;
  facebook?: string;
  equipo_trabajo?: any[];
}